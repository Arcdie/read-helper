const log = require('../libs/logger');

const memoryUsage = require('./memory-usage');


module.exports = async () => {
  // check memory
  /*
  setInterval(() => {
    memoryUsage();
  }, 10 * 1000); // 10 seconds
  */
};
